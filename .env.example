# Public team contact address, injected into every page as %VITE_TEAM_EMAIL%.
# VITE_* values are PUBLIC (they ship in the built HTML) — never put API keys,
# passwords, or personal secrets here. Use a team alias, not a personal inbox.
VITE_TEAM_EMAIL=team@example.org
